import express from 'express';
import type { Request, Response } from 'express';
import pinoHttp from 'pino-http';

const app = express();

// Fix for pino-http default import issue
app.use(pinoHttp({}));

app.get('/', (req: Request, res: Response) => {
  res.send('Hello World');
});

export default app;
